.background-pattern.border-bottom
  .container.text-center
    %h1
      Make your vote count in the #{election_date_season_type}!

    %h2.subdued
      Can you vote in the #{election_event_choice} on #{election_date_dm}?

    %h2.subdued
      Swap My Vote can help if there is a party you want vote for, but in
      %strong
        your
      constituency, voting for that party doesn't make sense.

= render partial: "swap_form"

= render partial: "news_sign_up_before_polls_close"
